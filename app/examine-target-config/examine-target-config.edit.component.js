'use strict';

angular.
  module('examineTargetConfigEdit').
  component('examineTargetConfigEdit', {
    templateUrl: 'examine-target-config/examine-target-config.edit.template.html',
    controller: ['$http', '$scope', 'ngProgressFactory', function examineTargetConfigEdit($http, $scope, ngProgressFactory) {
      var self = this;
    }]
  });