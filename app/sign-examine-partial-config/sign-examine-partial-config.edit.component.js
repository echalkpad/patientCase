'use strict'

angular.
  module('signExaminPartialConfigEdit').
  component('signExaminPartialConfigEdit', {
    templateUrl: 'sign-examine-partial-config/sign-examine-partial-config.edit.template.html',
    controller: ['$scope', '$http', 'ngProgressFactory', function signExaminPartialConfigEdit($scope, $http, ngProgressFactory){
      var self = this;
    }]
  });
