'use strict'

angular.
  module('signDisplayConfigEdit').
  component('signDisplayConfigEdit', {
    templateUrl: 'sign-display-config/sign-display-config.edit.template.html',
    controller: ['$scope', '$http', 'ngProgressFactory', function signDisplayConfigEdit($scope, $http, ngProgressFactory){
      var self = this;
    }]
  });